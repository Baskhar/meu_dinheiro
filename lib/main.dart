import 'package:flutter/material.dart';
import 'package:meu_dinheiro/app/app_module.dart';
import 'package:meu_dinheiro/app/app_widget.dart';

void main() {
  //classe principal do flutter, assim que o app for aberto, ele vai rodar
  //a classe que está dentro do método runApp
  runApp(AppModule());
}
